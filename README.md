# Minecraft Server en Docker

Este repositorio contiene la configuración necesaria para desplegar un servidor de Minecraft (Java Edition) altamente optimizado utilizando **Docker** y **Docker Compose**.

La estructura ha sido inicializada utilizando `docker init` y personalizada para utilizar la imagen de alto rendimiento de `itzg/minecraft-server`.
